# Core LangChain and LangGraph dependencies (updated for production readiness)
# Version 0.3.x provides better stability and Pydantic v2 support
langchain==0.3.7
langchain-core==0.3.15
langchain-community==0.3.7
langgraph==0.2.45

# AWS Bedrock support (production)
langchain-aws==0.2.6

# AWS SDK
boto3>=1.35.0
botocore>=1.35.0

# OpenAI support (for LM Studio compatibility)
langchain-openai==0.2.8

# Vector store and embeddings
faiss-cpu==1.8.0
tiktoken==0.8.0

# Document processing
langchain-text-splitters==0.3.2

# Pydantic for validation (LangChain 0.3.x uses Pydantic v2)
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0

# For production deployment - compatible with AWS Lambda Python 3.11 runtime
# Note: These versions are tested and compatible as of 2025-11
